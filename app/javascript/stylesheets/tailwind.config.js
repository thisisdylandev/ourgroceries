const colors = require('tailwindcss/colors')

module.exports = {
  theme: {
    extend: {
      colors: {
        grey: colors.blueGray,
        yellow: colors.amber,
        green: colors.emerald,
        purple: colors.violet
      },
    },
  },
  variants: {},
  plugins: [],
}